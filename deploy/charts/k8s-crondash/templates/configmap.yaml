apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ include "k8s-crondash.name" . }}
  labels:
    {{- include "k8s-crondash.labels" . | nindent 4 }}
data:
  CRONDASH_LISTEN_ADDR: {{ .Values.env.listenAddr | quote }}
  CRONDASH_NAMESPACE: {{ include "k8s-crondash.resolveNamespace" . | quote }}
  CRONDASH_REFRESH_INTERVAL: {{ .Values.env.refreshInterval | quote }}
  CRONDASH_JOB_HISTORY_LIMIT: {{ .Values.env.jobHistoryLimit | quote }}
